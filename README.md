### 자꾸 까먹어서 적어놓는 Git 초기 설정
```
git init

git config user.name "송문준"
git config user.email song960530@naver.com

git add .
git status
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/song960530/{레포지토리명}.git
git push -u origin main
```
